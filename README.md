# 🦊 Sly Fox Tunes

Telegram бот для скачивания медиа контента с YouTube с использованием Python 3.11+, aiogram 3.x и yt-dlp.

## ✨ Основные возможности

- 📥 Скачивание видео с YouTube в различных форматах и качестве
- 🎵 Скачивание только аудио дорожки
- 🔍 Поиск видео по текстовому запросу
- ⚡ Асинхронная обработка и отслеживание прогресса
- 🗃️ Автоматическая очистка временных файлов
- 📊 Статистика использования и управление пользователями

## 🚀 Быстрый старт

### Предварительные требования

- Python 3.11+
- [uv](https://docs.astral.sh/uv/) - современный пакетный менеджер Python

### Установка uv (если еще не установлен)

```bash
# macOS/Linux
curl -LsSf https://astral.sh/uv/install.sh | sh

# Windows (PowerShell)
powershell -c "irm https://astral.sh/uv/install.ps1 | iex"

# С помощью pip
pip install uv
```

### Настройка проекта

```bash
# Клонирование репозитория
git clone https://github.com/yourusername/sly-fox-tunes.git
cd sly-fox-tunes

# Создание виртуального окружения с Python 3.11
uv venv --python 3.11

# Синхронизация и установка всех зависимостей
uv sync

# Копирование и настройка переменных окружения
cp .env.example .env
# Отредактируйте .env файл, добавив ваш TELEGRAM_BOT_TOKEN
```

### Запуск

```bash
# Инициализация базы данных
uv run python scripts/init_db.py

# Запуск бота
uv run python main.py
```

## 📁 Структура проекта

```
sly-fox-tunes/
├── bot/                 # Основной код Telegram бота
├── services/            # Бизнес-логика (downloader, file_manager)
├── database/            # Модели и репозитории SQLAlchemy
├── config/              # Конфигурация приложения
├── utils/               # Вспомогательные утилиты
├── tests/               # Тесты
├── docs/                # Подробная документация
├── temp/                # Временные файлы (создается автоматически)
├── logs/                # Логи приложения (создается автоматически)
└── requirements.txt     # Python зависимости
```

## 🛠️ Разработка

### Установка dev зависимостей

```bash
uv add --dev pytest pytest-asyncio pytest-cov ruff black isort mypy
```

### Запуск тестов

```bash
uv run pytest tests/ --cov=bot --cov=services
```

### Проверка кода

```bash
# Форматирование и линтинг
uv run ruff check .
uv run black .
uv run isort .

# Проверка типов
uv run mypy .
```

### Работа с базой данных

```bash
# Создание новой миграции
uv run alembic revision --autogenerate -m "описание изменений"

# Применение миграций
uv run alembic upgrade head
```

### Добавление новых зависимостей

```bash
# Добавить основную зависимость
uv add package-name

# Добавить dev зависимость
uv add --dev package-name

# Синхронизировать после изменения requirements.txt
uv sync
```

## 🐳 Docker

```bash
# Сборка и запуск через Docker Compose
docker-compose up --build

# Только база данных для разработки
docker-compose up -d db redis
```

## 📚 Документация

Подробная документация находится в директории `docs/`:

- [Архитектура](docs/03-ARCHITECTURE.md) - Техническая архитектура системы
- [Технологический стек](docs/02-TECH-STACK.md) - Используемые технологии
- [Бизнес-процессы](docs/04-BUSINESS-PROCESSES.md) - Пользовательские сценарии
- [Схема базы данных](docs/05-DATABASE-SCHEMA.md) - Структура данных
- [План разработки](docs/06-DEVELOPMENT-PLAN.md) - Этапы реализации

## 🤝 Участие в разработке

1. Форк репозитория
2. Создание feature ветки (`git checkout -b feature/amazing-feature`)
3. Commit изменений (`git commit -m 'Add amazing feature'`)
4. Push в ветку (`git push origin feature/amazing-feature`)
5. Создание Pull Request

## 📄 Лицензия

Этот проект лицензирован под MIT License - см. файл [LICENSE](LICENSE) для деталей.

## 🙏 Благодарности

- [aiogram](https://github.com/aiogram/aiogram) - Telegram Bot framework
- [yt-dlp](https://github.com/yt-dlp/yt-dlp) - YouTube downloader
- [uv](https://github.com/astral-sh/uv) - Fast Python package manager