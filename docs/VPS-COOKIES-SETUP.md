# 🚀 Настройка cookies для VPS (без GUI браузера)

## Проблема
На VPS нет графического браузера, но YouTube требует cookies для обхода защиты от ботов.

## ⚡ Быстрое решение (5 минут)

### Шаг 1: Экспорт cookies на локальной машине

1. **Установите расширение браузера**:
   - **Chrome**: [Get cookies.txt LOCALLY](https://chromewebstore.google.com/detail/cclelndahbckbenkjhflpdbgdldlbecc)
   - **Firefox**: [cookies.txt](https://addons.mozilla.org/firefox/addon/cookies-txt/)

2. **Залогиньтесь в YouTube** в браузере

3. **Откройте любое видео** на YouTube

4. **Экспортируйте cookies**:
   - Нажмите на иконку расширения
   - Нажмите "Export" или "Export as cookies.txt"
   - Сохраните файл как `youtube_cookies.txt`

### Шаг 2: Загрузка на VPS

```bash
# С вашей локальной машины загрузите файл на сервер
scp youtube_cookies.txt root@your-server-ip:/root/projects/sly-fox-tunes/

# Или используйте конкретный путь
scp youtube_cookies.txt root@debian:/root/projects/sly-fox-tunes/youtube_cookies.txt
```

### Шаг 3: Настройка на VPS

```bash
# Подключитесь к VPS
ssh root@your-server-ip

# Перейдите в директорию проекта
cd /root/projects/sly-fox-tunes

# Убедитесь, что .env содержит правильную настройку
grep "COOKIES_FILE" .env || echo "COOKIES_FILE=youtube_cookies.txt" >> .env

# Проверьте конфигурацию
./scripts/setup_cookies_vps.sh
```

### Шаг 4: Перезапуск бота

```bash
# Остановите бота
pkill -9 -f "python main.py"

# Запустите заново
uv run python main.py
```

## 🧪 Проверка работы

На VPS выполните:
```bash
cd /root/projects/sly-fox-tunes
uv run python scripts/test_ytdlp_config.py "https://www.youtube.com/watch?v=M_lxIiJ8ck4"
```

Если видите `✓ Successfully extracted video info!` — всё работает! ✅

## 🔄 Обновление cookies (когда истекут)

YouTube периодически ротирует cookies. Когда снова появится ошибка "not a bot":

1. **На локальной машине**: Экспортируйте свежие cookies (Шаг 1)
2. **Загрузите на VPS**: `scp youtube_cookies.txt root@server:/path/`
3. **Перезапустите бот**: `pkill -9 -f "python main.py" && uv run python main.py`

## 📋 Автоматическая проверка

Используйте скрипт для диагностики:
```bash
./scripts/setup_cookies_vps.sh
```

Он проверит:
- ✅ Наличие файла cookies
- ✅ Корректность формата
- ✅ Конфигурацию .env
- ✅ Работу с YouTube

## ⚠️ Важно

- **Не коммитьте** `youtube_cookies.txt` в git (уже в `.gitignore`)
- Cookies содержат ваши авторизационные данные
- Срок действия cookies: обычно 1-3 месяца
- Храните локальную копию файла для быстрого обновления

## 🆘 Troubleshooting

### Ошибка: "Cookies file not found"
```bash
# Проверьте путь к файлу
ls -la youtube_cookies.txt

# Проверьте .env
cat .env | grep COOKIES
```

### Ошибка: "The provided YouTube account cookies are no longer valid"
**Решение**: Экспортируйте fresh cookies и загрузите заново

### Ошибка при загрузке по scp
```bash
# Используйте полный путь
scp -P 22 youtube_cookies.txt user@host:/full/path/youtube_cookies.txt

# С указанием порта SSH
scp -P 2222 youtube_cookies.txt user@host:/path/
```

## 💡 Совет

Создайте задачу для периодического обновления cookies (например, раз в месяц), чтобы бот всегда работал стабильно.
